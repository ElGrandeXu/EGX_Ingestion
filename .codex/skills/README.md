# Repo-scoped Codex skills

Primary repo-scoped Codex skill location for `EGX_Ingestion`.

Skills here are treated as the forward source for Codex behavior. `.agents/skills/` is kept as a compatibility mirror for older/current discovery paths. `skills/` is human documentation.

Current skills:

- `caveman-final-only`
- `source-ingestion`
- `repo-lab-test`
- `transversal-synthesis`

Maintenance:

- Update `.codex/skills/` first.
- Mirror to `.agents/skills/` only when compatibility requires it.
- Keep `SKILL.md` frontmatter with `name` and `description`.
