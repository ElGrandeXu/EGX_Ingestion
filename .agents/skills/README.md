# Active repo skills

Codex discovers repository skills from `.agents/skills/`.

This is the active location for `EGX_Ingestion` skills. The top-level `skills/` folder is a documentary mirror and registry, not the source that should drive Codex behavior when content diverges.

Active skills:

- `source-ingestion`
- `repo-lab-test`
- `transversal-synthesis`
